[phases.setup]
nixPkgs = ["python311", "ffmpeg"]

[phases.install]
cmds = [
  "pip install -r requirements.txt",
  "which ffmpeg && ffmpeg -version || apt-get install -y ffmpeg"
]

[start]
cmd = "python main.py"
